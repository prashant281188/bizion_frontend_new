export type hsn = {
    code: string
    description?: string
}

export type category = {
    category: string
    description: string
}

export type tax = {
    tax_name: string
    tax_value: number
}

export type unit = {
    unit: string
    unit_code: string
}

