import { SettingsIcon } from "lucide-react";
import React from "react";

const BiSettings = () => {
  return (
  <SettingsIcon
  size={20}
  className="cursor-pointer transition-transform duration-200 ease-in-out hover:rotate-45"
/>

  );
};

export default BiSettings;
